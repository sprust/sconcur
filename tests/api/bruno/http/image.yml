info:
  name: image
  type: http
  seq: 26

http:
  method: GET
  url: http://localhost:28080/image?name=sample.png
  params:
    - name: name
      value: sample.png
      type: query

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
