info:
  name: query
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:28080/query?foo=bar&x=1
  params:
    - name: foo
      value: bar
      type: query
    - name: x
      value: "1"
      type: query

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
