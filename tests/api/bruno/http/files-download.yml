info:
  name: files-download
  type: http
  seq: 25

http:
  method: GET
  url: http://localhost:28080/files/download?name=demo.bin
  params:
    - name: name
      value: demo.bin
      type: query

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
